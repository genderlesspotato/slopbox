[project]
name = "slopbox-temporal"
version = "0.1.0"
description = "Temporal workflow workers for slopbox"
requires-python = ">=3.11"
dependencies = [
    "temporalio",
    "slopbox-domain",
]

[tool.uv.sources]
slopbox-domain = { workspace = true }
